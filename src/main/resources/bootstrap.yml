spring:
    cloud:
        config:
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud, mysql
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-03-091a14f6d2dc}
            credentials:
                location: classpath:key.json    